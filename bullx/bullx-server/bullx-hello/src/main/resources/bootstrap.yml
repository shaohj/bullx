server:
  port: 18188

spring:
  application:
    name: @artifactId@
  security:
    user:
      name: bullx
      password: bullx
  cloud:
    config:
      enabled: false

# 注册中心
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://bullx:bullx@${EUREKA-HOST:bullx-eureka}:${EUREKA-PORT:18080}/eureka/
